#!/usr/bin/env bash
# 30-personal.sh — personal macOS defaults
# Add defaults write commands for personal machine preferences here.
# Use killall <App> at the end of any section that requires an app restart.

# Example: set a custom screenshot save location
# defaults write com.apple.screencapture location -string "$HOME/Pictures/Screenshots"
# killall SystemUIServer 2>/dev/null || true
