#!/usr/bin/env bash
# dotfiles-personal/profile.sh
# Sourced by the common installer after the profile repo is cloned.
# Sets profile identity; registers any personal-only package managers.

DOTFILES_PROFILE="personal"

# No custom package managers for the personal profile —
# brew, mas, and apt (defined in the common repo) cover everything.
